title: Установка

Реализуется с помощью целей сборки модуля из make-целей <Установка модуля в БД>.
Для корректной установки должна быть установлена актуальная версия OMS
( см. <Версия OMS-файлов>).

Параметры установки:
INSTALL_VERSION               - устанавливаемая версия модуля ( Last при
                                первоначальной установке или номер при
                                обновлении)
LOAD_OPERATORID               - логин и пароль оператора, выполняющего установку
                                ( формат: login/password)
LOAD_USERID                   - основной пользователь для установки в БД
                                ( формат: username/password@dbName)



group: 1.0.3
(<Версии::1.0.3 ( 2017-12-22)>)

Порядок установки:

- установка в основную схему;

(code)

make install INSTALL_VERSION=1.0.3 LOAD_USERID=search/???@??? LOAD_OPERATORID=???/???

(end code)



group: 1.0.2
(<Версии::1.0.2 ( 2017-06-08)>)

Порядок установки:

- установка в основную схему;

(code)

make install INSTALL_VERSION=1.0.2 LOAD_USERID=search/???@??? LOAD_OPERATORID=???/???

(end code)



group: Первоначальная установка

Предварительные требования:

- выдача прав на модуль Subversion пользователю search;
- выдача прав на модуль Option пользователю search;

Последовательность установки:

- выдача системных привилегий основной схеме, где расположен модуль ( на
  примере search@???);

(code)

make grant LOAD_USERID=sys/???@??? TO_USERNAME=search GRANT_SCRIPT=sys-privs LOAD_OPERATORID=???/???

(end code)

- установка основной части модуля ( на примере search@???);

(code)

make install LOAD_USERID=search/???@??? INSTALL_VERSION=Last LOAD_OPERATORID=???/???

(end code)

- выдача прав для возможности создания батчей основной схеме БД ( где
  установлен Scheduler);

(code)

make grant LOAD_USERID=???/???@??? GRANT_SCRIPT=batch TO_USERNAME=??? LOAD_OPERATORID=???/???

(end code)

- установка батчей модуля в основную схему БД;

(code)

make install LOAD_USERID2=???/???@??? INSTALL_VERSION=Last LOAD_OPERATORID=???/???

(end code)

- настройка опций батчей;

- установка ролей;

(code)

make install LOAD_USERID3=???/???@??? INSTALL_VERSION=Last LOAD_OPERATORID=???/???

(end)

- настройка опций (
  <pkg_SvnSearcherBase::Наименования опций для соединения с репозиторием>
  , <pkg_SvnSearcherBase::Наименования опций для настройки индексирования>
  );


group: Выдача прав на использование модуля

(code)

make grant LOAD_USERID=search/???@??? TO_USERNAME=??? LOAD_OPERATORID=???/???

(end)



